# ChatFlow Environment Variables Configuration
# 
# Copy this file to .env in the root directory and fill in your actual API keys
# 
# ==============================================================================
# API KEYS CONFIGURATION
# ==============================================================================
# You can add up to 10 API keys. Each API key needs:
# - A name (e.g., "Primary", "Secondary", "Work", "Personal")
# - The actual API key from OpenRouter (https://openrouter.ai/)
#
# Format:
# VITE_API_KEY_[NUMBER]_NAME=YourKeyName
# VITE_API_KEY_[NUMBER]=your_actual_api_key_here

# Example API Key 1 (Required - at least one API key is needed)
VITE_API_KEY_1_NAME=Primary
VITE_API_KEY_1=your_actual_api_key_here

# Example API Key 2 (Optional - add more as needed)
# VITE_API_KEY_2_NAME=Secondary
# VITE_API_KEY_2=another_api_key_here

# You can continue adding more API keys up to VITE_API_KEY_10

# ==============================================================================
# MODEL FILTERING (OPTIONAL)
# ==============================================================================
# By default, all models available for each API key will be shown.
# To filter and show only specific models, uncomment and list model IDs below:
#
# You can specify up to 20 models:
# VITE_MODEL_1=openai/gpt-4-turbo
# VITE_MODEL_2=anthropic/claude-3-opus
# VITE_MODEL_3=google/gemini-pro
#
# If no models are specified, all available models will be shown.
# To find model IDs, visit: https://openrouter.ai/models

# Example Model Filters (uncomment to use)
# VITE_MODEL_1=openai/gpt-4-turbo
# VITE_MODEL_2=anthropic/claude-3-sonnet

# You can continue adding more models up to VITE_MODEL_20

# ==============================================================================
# NOTES:
# ==============================================================================
# - All variables MUST start with VITE_ to be accessible in the app
# - Never commit your .env file to version control (it should be in .gitignore)
# - After updating .env, restart the development server for changes to take effect
# - API keys are automatically validated - invalid keys will show error messages
# - Each API key will automatically fetch its available models from OpenRouter
